import React from 'react';

const HomeNoToken = () => {
  return (
    <div className="login-box">
      <h3>
        You have no registry token. 
        Contact a teacher for an invitation to a class 
        and click the link in the email received.
      </h3>
    </div>
  );
};

export default HomeNoToken;